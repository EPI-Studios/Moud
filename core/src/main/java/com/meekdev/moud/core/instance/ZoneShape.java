package com.meekdev.moud.core.instance;

// the shape of a zone. a polygon is the outline its Attachment children draw on its own floor, as tall
// as its size says
public enum ZoneShape {
    BOX,
    SPHERE,
    CYLINDER,
    POLYGON
}
