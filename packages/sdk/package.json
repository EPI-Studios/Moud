{
  "name": "@epi-studio/moud-sdk",
  "version": "0.1.1-alpha",
  "description": "The official Scripting Development Kit for the Moud Engine.",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp src/index.d.ts dist/index.d.ts"
  }
}