{
  "name": "moud-cli",
  "version": "0.1.0-alpha",
  "description": "The official command-line interface for developing things using MOud",
  "type": "module",
  "bin": {
    "moud": "dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=esm --external:esbuild"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "chokidar": "^3.6.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "esbuild": "^0.21.4"
  }
}